{
  "name": "techx-todo-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Task Manager TechX — monorepo (backend Fastify + frontend Angular + MySQL)",
  "engines": {
    "node": ">=22.22.3",
    "npm": ">=9"
  },
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install"
  }
}
